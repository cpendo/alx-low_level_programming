#include "main.h"

/**
 * _islower - checks for lowercase characters
 *
 * Return: 1 if character is successful
 */

int _islower(int c)
{
	if (c >= 97 && c <= 122)
		Return (1);
	else
		Return (0);
}
